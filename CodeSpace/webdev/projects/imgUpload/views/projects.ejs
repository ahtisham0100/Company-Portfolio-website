<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= projectName %> - Project Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-oYXK1tIOHRP9S6Sr2ljNQW6Lmgp19gq6NodexzLBY5klw+q5pum2OHvQw6cg9BMs" crossorigin="anonymous">

    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .project-header {
            text-align: center;
            margin-bottom: 20px;
        }
        .images {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
        }
        .images img {
            max-width: 700px;
            border-radius: 8px;
        }
        .description {
            margin-top: 20px;
            outline: 1px solid salmon;
            padding: 1rem;
        }
        .description p {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 20px;
            line-height: 2;
background-color: antiquewhite;
        }
    </style>
</head>
<body>
    <div class="project-header">
        <h1>Project: <%= projectName %></h1>
    </div>

    <% if (images.length > 0) { %>
        <div class="images container">
            <% images.forEach(image => { %>
                <img src="/uploads/<%= image.imageaddress %>" alt="Image for <%= projectName %>" />
                <% }) %>
        </div>
    <% } else { %>
        <p>No images available for this project.</p>
    <% } %>

    <% if (description.length > 0) { %>
        <div class="container description ">
            <% description.forEach(desc => { %>
                <p><%= desc.paragraph %></p>
            <% }) %>
        </div>
    <% } else { %>
        <p>No descriptions available for this project.</p>
    <% } %>

</body>
</html>
