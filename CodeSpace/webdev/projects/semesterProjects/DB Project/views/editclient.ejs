<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <title><%= client ? 'Edit Client' : 'Add Client' %></title>
    
</head>
<body>
    <h1><%= client ? 'Edit Client' : 'Add Client' %></h1>
    <form action="/clients/save" method="POST">
        <% if (client) { %>
            <input type="hidden" name="client_id" value="<%= client.client_id %>">
        <% } %>
        <label for="client_name">Client Name:</label>
        <input type="text" id="client_name" name="client_name" value="<%= client ? client.client_name : '' %>" required>
        <br>
        <label for="contact_name">Contact Name:</label>
        <input type="text" id="contact_name" name="contact_name" value="<%= client ? client.contact_name : '' %>" required>
        <br>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= client ? client.email : '' %>" required>
        <br>
        <label for="phone">Phone:</label>
        <input type="text" id="phone" name="phone" value="<%= client ? client.phone : '' %>" required>
        <br>
        <button type="submit"><%= client ? 'Update' : 'Add' %></button>
    </form>
    <a href="/clients">Back to List</a>
</body>
</html>
