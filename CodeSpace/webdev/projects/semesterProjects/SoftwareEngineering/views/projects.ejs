<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= projectName %> - Project Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-oYXK1tIOHRP9S6Sr2ljNQW6Lmgp19gq6NodexzLBY5klw+q5pum2OHvQw6cg9BMs" crossorigin="anonymous">

    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .project-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .project-header h1 {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .project-item {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
        }

        .image {
            flex: 1 1 50%;
            max-width: 500px;
            text-align: center;
        }

        .image img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .description {
            flex: 1 1 45%;
            max-width: 500px;
            padding: 20px;
            background-color: #025bee;
            border-radius: 8px;
            box-shadow: 0 10px 18px rgba(0, 0, 0, 0.671);
            line-height: 1.8;
            font-size: 18px;
            color: #f4f4f4;
        }

        .description p {
            margin-bottom: 15px;
        }

        @media (max-width: 768px) {
            .project-item {
                flex-direction: column;
                align-items: center;
            }

            .image,
            .description {
                flex: 1 1 80%;
            }
        }
    </style>
</head>
<body>
    <div class="project-header">
        <h1>Project: <%= projectName %></h1>
    </div>
    
    <% if (images.length > 0 || description.length > 0) { %>
        <div class="container">
            <% 
            const maxLength = Math.max(images.length, description.length);
            for (let i = 0; i < maxLength; i++) { 
            %>
                <div class="project-item">
                    <% if (images[i]) { %>
                        <div class="image">
                            <img src="/uploads/<%= images[i].imageaddress %>" alt="Image for <%= projectName %>" />
                        </div>
                    <% } %>
                    
                    <% if (description[i]) { %>
                        <div class="description">
                            <p><%= description[i].paragraph %></p>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </div>
    <% } else { %>
        <p>No images or descriptions available for this project.</p>
    <% } %>
    
</body>
</html>
